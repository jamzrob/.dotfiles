#!/bin/zsh

DIR=$HOME/.dotfiles

DOTFILES=(
    ".zshrc"
    ".tmux.conf"
    ".config/nvim"
    ".config/tmuxinator"
    ".config/skhd"
    ".ssh/config"
    ".config/yabai/.yabairc"
)

for dotfile in "${DOTFILES[@]}";do
	ln -sfn "${DIR}/${dotfile}" "${HOME}/${dotfile}"
done
