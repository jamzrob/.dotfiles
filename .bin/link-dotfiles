#!/bin/zsh

DIR=$HOME/.dotfiles


DOTFILES_HOME=(
    ".zshrc"
    ".config/tmux/.tmux.conf"
    ".config/.gitconfig"
    ".config/nvim"
    ".config/tmuxinator"
    ".config/skhd"
    ".ssh/config"
    ".config/yabai/.yabairc"
)

for dotfile in "${DOTFILES[@]}";do
	rm -rf "${HOME}/${dotfile}"
	ln -sf "${DIR}/${dotfile}" "${HOME}/${dotfile}"
done
