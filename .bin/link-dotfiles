#!/bin/zsh

DIR=$HOME/.dotfiles

DOTFILES=(
    ".zshrc"
    ".tmux.conf"
    ".ssh/config"
    ".config/nvim"
    ".config/tmuxinator"
)

for dotfile in "${DOTFILES[@]}";do
        ln -sfn "${DIR}/${dotfile}" "${HOME}/${dotfile}"
done
